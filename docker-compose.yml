services:
  postgres:
    image: postgres:16
    container_name: spendgrove-db
    environment:
      POSTGRES_DB: spendgrove
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: devpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: